# Code Style
Adhere to DRY. Use abstractions as a default, unless they are unnecessary.
I prefer over-engineered code to under-engineered code.
Readability is a must. main.ipynb should be de-cluttered, and it should only call and import structures defined in other .py files.

# Context
I am writing a open-source small repo for research, which deals with different techniques of measuring similarity of semantic relationship graphs constructed from various phrases. This is only context: perform the actions below as instructed.